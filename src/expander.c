#include "minishell.h"

void expand(t_string *s, char **env, int exit_code)
{
	//ver se existe $ ao procurar em env
	//se sim substituir
	//se não
	//ver se é $?
	//se sim substituir pormake && ./minishell
	//
}
